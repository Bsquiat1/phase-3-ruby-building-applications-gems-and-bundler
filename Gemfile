# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem 'hashie'
gem 'sinatra', '2.0.2'
gem 'octokit', '~> 2.0'
gem 'awesome_print', git: 'git@github.com:awesome-print/awesome_print.git'
group :development do
  gem 'pry'
end
group :test do
  gem 'rspec' do
    # Specify any additional configurations for the rspec gem within the block if needed
  end
end
